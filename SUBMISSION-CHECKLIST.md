# Чек-лист сдачи проекта модуля 2

Требования из **всех** источников в одном месте: страница проекта (шаги 1–6), уроки модуля
и комментарии наставника. Каждый пункт — дословно или близко к тексту, откуда взят и чем
подтверждён. «Чем подтверждено» — ссылка на прогон, issue или PR, а не утверждение.

Зачем файл: сдача 20.09 вернулась, потому что проверка шла по одному источнику — тексту
шага 6. Требование автотриажа и авторевью было в уроке «Комбинирование workflow» и в
комментарии наставника, а в тексте шага его нет. Любой новый комментарий наставника сразу
становится строкой здесь.

Обозначения: ✅ выполнено и подтверждено · ⚠️ выполнено с оговоркой · ⬜ не выполнено.

## Страница проекта

| # | Требование | Чем подтверждено | Статус |
|---|---|---|---|
| 1.1 | Issue с расплывчатым описанием, разбор через `/oc explain` | issue #3 → разбор с причиной в `server/index.js` | ✅ |
| 1.2 | Следующий шаг — план или fix через `/oc fix` | PR #4 | ✅ |
| 2.1 | OpenCode подключён, вызов из issue работает | issue #3, workflow `opencode.yml` | ✅ |
| 3.1 | Issue прошёл через triage с участием агента | issue #3 (вручную), #20 и #21 (автоматически) | ✅ |
| 4.1 | Агент готовит PR, связанный с issue | PR #4, #8, #10 | ✅ |
| 4.2 | Два вида замечаний: общий комментарий и комментарий к строке | PR #10 | ✅ |
| 4.3 | Агент обновил PR и учёл оба вида обратной связи | PR #10 — на PR #4 правки пришлось внести человеку, поэтому цикл повторён | ✅ |
| 4.4 | Коммиты агента по Conventional Commits | **частично:** в каждом из PR #4, #8, #10 у агента есть коммит без префикса («SEO исправлен: …», «`EventTypesPage` возвращён …»). В `main` попадает заголовок PR (squash-мерж), он по формату — поэтому changelog собирается верно. Правило формата есть в `AGENTS.md`, но агент ему следует не всегда | ⚠️ |
| 4.5 | release-please создал release-PR с changelog и версией | PR #1 → v1.0.0, PR #17 → v1.0.1 | ✅ |
| 5.1 | Повторяющаяся задача по расписанию + ручной запуск | `lighthouse-nightly.yml`: `schedule` и `workflow_dispatch` | ✅ |
| 5.2 | Lighthouse CLI, отчёт сохраняется и доступен утром | артефакт `lighthouse-<run_id>`, 30 дней | ✅ |
| 5.3 | По итогам отчёта зафиксированы нужные правки | issues #5, #11, #12; правки — #7/PR #8, #9/PR #10 | ✅ |
| 6.1 | Агент запускается только на целевые команды, нет самозапуска и реакции на ботов | `if:` в `opencode.yml`: команда + `author_association` + `user.type != 'Bot'`; прогоны от ответов бота — `skipped` | ✅ |
| 6.2 | У каждого workflow минимальные права, нет лишних write | секции `permissions:` во всех workflow, таблица в `AGENT-PROCESS.md` | ✅ |
| 6.3 | Разумная частота schedule, модель под задачу, понятно где смотреть run'ы | еженедельный cron; модель одна — без карты доступна только `big-pickle` | ⚠️ |
| 6.4 | Хотя бы один запуск scheduled workflow | события `schedule`: 07.09, 14.09, 21.09 (runs 34098925630, 34824303714, 35579654329) | ✅ |
| 6.5 | Самооценка: сколько задач с первого прохода, где были итерации | раздел «Самооценка» в `AGENT-PROCESS.md` | ✅ |

## Уроки модуля

| # | Требование | Источник | Чем подтверждено | Статус |
|---|---|---|---|---|
| У.1 | Отдельный workflow на сценарий: автотриаж новых issue | «Комбинирование workflow» (`opencode-triage.yml`) | `triage.yml`, прогоны на #20 и #21 | ✅ |
| У.2 | Автоматическое ревью PR с prompt'ом | «Автоматический ревью при открытии PR» (`opencode-review.yml`) | `review.yml`, прогоны на PR #19 и #22 | ✅ |
| У.3 | Ревью на `opened, synchronize, reopened, ready_for_review` | тот же урок | триггер в `review.yml` | ✅ |
| У.4 | Workflow работают независимо друг от друга | «Комбинирование workflow», практика | триаж, ревью, `/oc`, Lighthouse — разные файлы, агенты и права | ✅ |
| У.5 | Модель под задачу: дешёвая для триажа, сильная для ревью | «Комбинирование workflow» | нет: без карты доступна только `big-pickle` | ⚠️ |

## Комментарии наставника

**Возврат 21.09** («намного лучше, но не супер»):

| # | Замечание | Чем закрыто | Статус |
|---|---|---|---|
| Н.1 | Нет workflow автотриажа (`issues`) и авторевью (`pull_request`) с prompt | PR #19; проверено на issue #20, #21 и PR #22 | ✅ |
| Н.2 | `.claude/skills/db-layer` и `new-endpoint` описывают старую схему из 4 таблиц | PR #22 — оба скилла переписаны по коду | ✅ после мержа #22 |
| Н.3 | `AGENTS.md:36`: «прод гасит засев через `render.yaml`», а там `true` | PR #22; та же ошибка найдена и исправлена в `README.md` | ✅ после мержа #22 |
| Н.4 | `AGENTS.md:68`: абсолютный путь к личному Obsidian | PR #22 — путь вынесен в `CLAUDE.local.md` вне репозитория | ✅ после мержа #22 |
| Н.5 | Не только починить, но и описать изменения процесса | `AGENT-PROCESS.md`, раздел «Автотриаж и авторевью — что изменилось в процессе» | ✅ |

## Что сознательно не выполнено

- **Модель под задачу** (6.3, У.5) — без привязанной карты OpenCode Zen отдаёт только
  `big-pickle`. Разные модели не проверены.
- **Фильтр чужих авторов в триаже** — не проверен прогоном: для этого нужен второй аккаунт.
- **Ревью PR, открытого агентом** — по замыслу `review.yml` ревьюит и их (отличие от урока),
  но таких PR после его появления ещё не было: запуск и проверку прав бота экшеном
  не подтверждены.
