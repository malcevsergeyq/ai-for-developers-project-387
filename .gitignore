# Зависимости
node_modules/

# Переменные окружения — DATABASE_URL и прочие секреты
.env
.env.local
.env.*.local

# Логи
*.log
npm-debug.log*

# Сборка фронта
ui/dist/

# Служебное
.DS_Store
Thumbs.db

# Артефакты Playwright
playwright-report/
test-results/
blob-report/
.playwright-mcp/

# Отчёты Lighthouse: генерируются ночным прогоном в рабочей директории и уезжают
# артефактом. В репозитории им делать нечего — 11 тысяч строк на каждый прогон.
# Правило в промпте агент уже нарушил (PR #6), поэтому запрет технический.
lighthouse-report.*
