# План и статус проекта

«Запись на звонок» — упрощённый сервис бронирования по мотивам Cal.com (вариант проекта «Календарь звонков», модуль 1). Гость выбирает тип события и свободный слот и записывается без регистрации; владелец календаря создаёт типы событий и смотрит список предстоящих встреч. Раздельные фронт и бэк по требованию задания, взаимодействие — только через API-контракт.

## Смена ТЗ — 2026-08-19

Урок проекта в курсе был переписан: вместо прежних этапов теперь **6 шагов**, и три вещи изменились принципиально.

| | Было (до 19.08) | Стало |
|---|---|---|
| Контракт | рукописный `API-CONTRACT.md` | TypeSpec → OpenAPI (`spec/`) |
| Хранилище | PostgreSQL + `schema.sql` | ТЗ: «отдельная база не нужна, достаточно хранилища в памяти» |
| Порядок | бэк первым | фронт (шаг 3) → бэк (шаг 4), фронт разрабатывается против мока |
| Домен | расписания доступности, таймзоны, статусы броней, токены | окно жёстко 14 дней, слоты 30 минут, без статусов и токенов |
| Финал | не был описан | Playwright e2e + CI + release-please + Docker + публичный деплой |

**Выпало из скоупа** (решение 19.08 — фиксируем контракт строго по ТЗ урока): расписания доступности и их правила, таймзоны (`AvailabilitySchedule.timezone`), `hidden` как фильтр публичности, адресация по `slug`, непрозрачный `token` для доступа к брони, статусы `pending`/`confirmed`/`cancelled`/`completed`, мягкая отмена, правило «один email — одна активная бронь на дату» (было правило 12), запрет `DELETE` при связанных записях.

Старые правила не потеряны — они в git до коммита `a28bc4d` и в файле `API-CONTRACT.md`, удалённом 19.08 (`git show HEAD:API-CONTRACT.md`). Часть из них — хорошие кандидаты в раздел «Что дальше» урока, если проект будет развиваться после сдачи.

## Стек

- Бэк: Node + Express — `server/`
- Контракт: TypeSpec — `spec/`
- Фронт: React 19 + TypeScript + Vite — `ui/`; shadcn/ui (стиль `base-nova`: Base UI + Tailwind v4), `react-router-dom`. Vue отклонён осознанно: shadcn/ui и Mantine существуют только под React, а Vue-порт лишает смысла shadcn MCP из урока
- Хранилище: PostgreSQL при заданном `DATABASE_URL`, иначе память — две реализации за одним интерфейсом репозиториев

## API-контракт

Источник истины — [`spec/main.tsp`](./spec/main.tsp) (TypeSpec). `spec/openapi.yaml` из него **генерируется**, руками не правится: `npm run spec:build`.

Семь операций:

| Метод | Путь | Кто | Ответы |
|---|---|---|---|
| `GET` | `/event-types` | гость | `200` |
| `GET` | `/event-types/{id}` | гость | `200`, `404` |
| `GET` | `/event-types/{id}/slots` | гость | `200`, `404` |
| `POST` | `/bookings` | гость | `201`, `400`, `404`, `409` |
| `GET` | `/admin/event-types` | владелец | `200` |
| `POST` | `/admin/event-types` | владелец | `201`, `400` |
| `GET` | `/admin/bookings` | владелец | `200` |

## Ключевые решения (Design First, 2026-08-19)

Урок задаёт жёстко: типы событий с `durationMinutes`, слоты по 30 минут на 14 дней от текущей даты, занятость **глобальная по времени** — две записи на пересекающееся время запрещены даже для разных типов событий.

Урок молчит, решено нами:

- **Сетка стартов 30 минут, бронь занимает `durationMinutes`.** Событие на 60 минут занимает два соседних старта и блокирует их для всех типов. Альтернатива «все события ровно 30 минут» отвергнута: она обесценивает поле `durationMinutes` и убирает единственный интересный случай — пересечение интервалов разной длины. Переиспользуется `server/services/intervals.js`
- **Рабочее окно 09:00–18:00 UTC, только будни.** 18 слотов в день; календарь на фронте выглядит правдоподобно, данных на порядок меньше, чем при круглосуточной сетке
- **Всё время — UTC, ISO 8601.** Таймзон ТЗ не требует, поэтому их нет вовсе — прежнее решение про IANA-таймзону в расписании снято вместе с расписаниями
- **`id` — непрозрачные строки (UUID).** Так в примере урока; заодно исчезает баг из аудита 15.08, где `id` больше `int4` доходил до Postgres и превращался в `500` вместо `404`
- **Слоты не хранятся, а вычисляются** из окна, сетки и существующих броней. Хранить нечего: окно скользит вместе с текущей датой
- **Слот вне окна или мимо сетки → `400`, занятый слот → `409`.** Разные классы: первое — неверный вход, второе — конфликт состояния. Проверка Hexlet требует именно понятного сообщения о конфликте
- **Формат ошибки `{error, message}`** сохранён от прежнего контракта — конвенция проекта не изменилась
- **У владельца только создание типов событий и просмотр встреч.** Ни `PATCH`, ни `DELETE`: ТЗ говорит «создаёт типы событий», остальное — сверх задания
- **`eventTypeTitle` дублируется в `Booking`**, чтобы список владельца рисовался одним запросом без джойна на фронте

## Статус по шагам урока

| # | Шаг | Статус |
|---|---|---|
| 1 | Cal.com: регистрация и разбор сценария | ✅ 2026-08-03 |
| 2 | Проектирование: TypeSpec-контракт | ✅ 2026-08-19 — `spec/main.tsp` компилируется, `spec/openapi.yaml` сгенерирован, покрытие сценариев проверено |
| 3 | Фронтенд | ✅ 2026-08-19 — React + TS + Vite + shadcn/ui, типы API сгенерированы из контракта, обе роли реализованы, путь записи пройден против мока Prism |
| 4 | Бэкенд по контракту | ✅ 2026-08-19 — все 7 операций, генерация слотов, глобальная занятость, 55 тестов; код прежнего скоупа снесён. Postgres-репозиторий написан, но против живой базы не исполнялся |
| 5 | Интеграционные сценарии: Playwright + CI + release-please | ✅ 2026-08-19 — 4 e2e-сценария (`e2e/`), CI зелёный на GitHub, release-please открыл PR #6 «release 1.0.0» с changelog |
| 6 | Docker и деплой на Render/Railway | ✅ 2026-08-19 — образ 245 МБ, деплой на Render по `render.yaml`, https://calendar-booking-y3dz.onrender.com. 4 e2e-сценария прогнаны и против контейнера, и против продакшена |

## Проверки

| Что | Чем | Сколько |
|---|---|---|
| Контракт компилируется, `openapi.yaml` не разошёлся | `npm run spec:build` + `git diff --exit-code` в CI | — |
| Правила предметной области, валидация, коды ошибок | Vitest + supertest, `server/tests/` | 55 |
| Пользовательские сценарии в браузере | Playwright, `e2e/` | 4 |
| Форма кода | ESLint (бэк), oxlint (фронт) | — |
| Те же сценарии против контейнера и продакшена | `E2E_BASE_URL=… npm run test:e2e` | 4 |

Что именно покрывают e2e и почему выбраны эти сценарии — в [`e2e/SCENARIOS.md`](./e2e/SCENARIOS.md).
Ключевой из них — конфликт слота: 19.08 этот случай сломался при полностью зелёных юнит-тестах,
потому что ломался стык фронта и бэка, а не слой. Тест проверен на отзывчивость — с возвращённым
багом он краснеет.

## Долг от прежнего скоупа — закрыт 2026-08-19

Расписания доступности, их сервис, роуты, маппер и тесты удалены. `schema.sql` переписан
под новый контракт. `intervals.js` заменён на `time.js`: функция пересечения интервалов
переехала как есть, арифметика `HH:MM` внутри суток уступила место работе с моментами UTC.

Четыре бага из аудита 15.08 закрыты: переполнение `id` невозможно (идентификаторы —
строки), catch-all отдаёт `{error, message}`, `entity.too.large` и `encoding.unsupported`
получили свои коды вместо 500, `cors()` принимает список origin из `CORS_ORIGIN`.

## Решено 2026-08-30

- **Демо-данные убраны за флаг окружения `SEED_DEMO`** (было: «Решено 2026-08-19 …» в открытых вопросах). Засев включается при любом значении, кроме `false`, — так он по умолчанию работает в локальном `npm start` и в контейнере проверки Hexlet (на него `render.yaml` не применяется). Продакшен на Render подавляет засев `SEED_DEMO=false` через `render.yaml`: чистый деплой больше не показывает «чужие» записи.

## Открытые вопросы

- **Postgres так и не поднят — подъём отложен осознанно (решение 2026-08-19).** Идём в шаг 5 с непроверенной pg-веткой. Что это значит на практике: приложение проходит проверку и работает на in-memory, но если на деплое задать `DATABASE_URL`, оно уйдёт в код, который не исполнялся ни разу — и упадёт уже в проде, а не в тестах. Поэтому деплой с `DATABASE_URL` включать нельзя, пока база не поднята и схема не накатана. Не проверены: exclusion-ограничение `bookings_no_overlap`, `gen_random_uuid()`, приведение `id::text` и перевод SQLSTATE `23P01` в 409.
- **`/admin/*` ничем не защищён** — авторизация вынесена ТЗ за скоуп, так и остаётся
- **Демо-данные засеваются при пустом хранилище** (два типа встреч в `server/index.js`). Закрыто 2026-08-30: засев вынесен за флаг `SEED_DEMO` (включён по умолчанию, продакшен подавляет через `render.yaml`) — см. «Решено 2026-08-30»

## Следующие шаги

1. Решить, что делать с release-PR #6: changelog первого релиза собран по всей истории и перечисляет в том числе фичи отменённого скоупа (расписания доступности, правило 12), которых в коде больше нет
2. Поднять Postgres в Docker и накатить `schema.sql` — единственный способ проверить pg-ветку
